services:
  - type: web
    name: nyc-affordability-calculator
    env: node
    buildCommand: cd nyc-affordability-calculator && npm install
    startCommand: cd nyc-affordability-calculator && npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
      - key: GOOGLE_PLACES_API_KEY
        sync: false
